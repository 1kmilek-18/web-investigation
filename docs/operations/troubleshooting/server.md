# サーバー管理・再起動

**統合元:** docs/RESTART_SERVER.md  
**最終更新:** 2026-02-03

---

## 再起動手順

1. 開発サーバーが動いているターミナルで `Ctrl+C` で停止
2. `npm run dev` で再起動
3. 「Ready」表示後に http://localhost:3000/api/health で確認

詳細は **docs/RESTART_SERVER.md** を参照。
