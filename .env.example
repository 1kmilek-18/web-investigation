# PostgreSQL (Prisma) — MCP 接続先と同じ Supabase を使う場合
# Dashboard: https://supabase.com/dashboard → プロジェクト (ref: eanxmobszkkkiemdvjbm) → Settings → Database → Connection string (URI) をコピー
DATABASE_URL="postgresql://postgres.[PROJECT_REF]:[YOUR-PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Supabase（MCP 接続先・アプリ連携用・上記と同じプロジェクト）
NEXT_PUBLIC_SUPABASE_URL="https://eanxmobszkkkiemdvjbm.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

# 日次ジョブトリガー保護 (POST /api/cron/daily, POST /api/jobs/manual, POST /api/jobs/stop)
CRON_SECRET=""

# Claude API (Anthropic) - Phase 2: 要約機能で使用
ANTHROPIC_API_KEY=""

# Gmail送信 (Phase 3: 配信機能で使用)
# Gmailアプリパスワードの取得方法:
# 1. Googleアカウントで2段階認証を有効化
# 2. https://myaccount.google.com/apppasswords でアプリパスワードを生成
# 3. 16文字のパスワードを設定
GMAIL_USER="your-app@gmail.com"
GMAIL_APP_PASSWORD="your-16-char-app-password"
